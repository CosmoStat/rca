* Example data for running RCA *

Simply run

$ ./rca

in the root directory with the configuration file provided (rca_config.ini). This will produce a filed called est_psfs.fits containing the super-resolved PSFs in the example directory.